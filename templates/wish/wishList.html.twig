{% extends 'base.html.twig' %}

{% block body %}
    <h1>Liste des souhaits</h1>
    <div class="col-4">
        <table class="table table-striped table-hover">
            <thead>
            <tr>
                <th>#</th>
                <th>Intitulé</th>
            </tr>
            </thead>

            <tbody>
            {% for wish in wishes %}
                <tr class="wishList-line" onclick="document.location='{{ path('wish', {id: wish.id}) }}'">
                    <td>{{ loop.index }}</td>
                    <td>{{ wish.title }}</td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>
{% endblock %}
